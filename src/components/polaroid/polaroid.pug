mixin polaroid
  section.section.polaroid-section#polaroids(aria-labelledby="polaroid-title")
    p.section__eyebrow(data-reveal, data-polaroid-eyebrow)
    h2#polaroid-title.section__title(data-reveal)
    p.section__lead(data-reveal, data-polaroid-lead)
    .polaroid(data-polaroid, data-reveal)
