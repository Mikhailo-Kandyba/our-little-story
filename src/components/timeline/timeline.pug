mixin timeline
  section.section.timeline-section#timeline(aria-labelledby="timeline-title")
    p.section__eyebrow(data-reveal, data-timeline-eyebrow)
    h2#timeline-title.section__title(data-reveal)
    p.section__lead(data-reveal, data-timeline-lead)
    .timeline(data-timeline)
