mixin story
  section.section.story#story(aria-labelledby="story-title")
    p.section__eyebrow(data-reveal, data-story-eyebrow) Початок
    h2#story-title.section__title(data-reveal)
    .story__copy
