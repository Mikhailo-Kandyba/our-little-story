mixin closing
  section.section.closing#closing(aria-labelledby="closing-title")
    .closing__inner
      h2#closing-title.closing__title(data-reveal)
      .closing__copy
      p.closing__finale-text(data-reveal, data-closing-finale)
      p.closing__sign(data-reveal, data-closing-sign)
