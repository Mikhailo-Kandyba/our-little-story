mixin mood
  section.section.mood-section#mood(aria-labelledby="mood-title")
    p.section__eyebrow(data-reveal) Настрій
    h2#mood-title.section__title(data-reveal)
    p.section__lead(data-reveal, data-mood-lead)
    .mood(data-mood, data-reveal)
