mixin quiz
  section.section.quiz-section#quiz(aria-labelledby="quiz-title")
    p.section__eyebrow(data-reveal, data-quiz-eyebrow)
    h2#quiz-title.section__title(data-reveal)
    .quiz(data-quiz, data-reveal)
