mixin carousel
  section.section.carousel-section#memories(aria-labelledby="carousel-title")
    p.section__eyebrow(data-reveal, data-carousel-eyebrow)
    h2#carousel-title.section__title(data-reveal)
    p.section__lead(data-reveal, data-carousel-lead)
    .carousel(data-carousel, data-reveal)
